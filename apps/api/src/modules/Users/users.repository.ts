import { prisma } from "@repo/db";
import { User } from "./user.type.js";

export class UserRepo {
    constructor(user: User) {}
    async create() {
        try {
            
        } catch (error) {
            
        }
    }
}