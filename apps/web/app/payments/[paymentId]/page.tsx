"use client"
import { useEffect,useState } from "react"
import Link from "next/link"
import { ArrowLeft,CheckCircle2,Clock3,CreditCard,XCircle } from "lucide-react"
import { useParams } from "next/navigation"
import { PageShell } from "@/components/dashboard/page-shell"
import { Badge } from "@/components/ui/badge"
import { buttonVariants } from "@/components/ui/button"
import { Card } from "@/components/ui/card"
import { apiClient } from "@/lib/api"
import { formatNaira } from "@/lib/dashboard-data"
export default function PaymentDetailsPage(){const {paymentId}=useParams<{paymentId:string}>();const [payment,setPayment]=useState<any>();useEffect(()=>{apiClient.payment(paymentId).then(setPayment).catch(()=>setPayment(false))},[paymentId]);if(payment===false)return <PageShell title="Payment not found"><Card className="p-6"><p className="text-sm text-muted-foreground">The payment could not be found.</p><Link href="/payments" className={buttonVariants({variant:"outline",className:"mt-4 rounded-xl"})}><ArrowLeft/> Back to payments</Link></Card></PageShell>;if(!payment)return null;const Icon=payment.status==="PAID"?CheckCircle2:payment.status==="FAILED"?XCircle:Clock3;return <PageShell title="Payment details"><Link href="/payments" className={buttonVariants({variant:"ghost",className:"w-fit px-0"})}><ArrowLeft/> Payment history</Link><Card className="items-center p-7 text-center"><div className="flex size-16 items-center justify-center rounded-full bg-accent"><Icon className="size-8 text-primary"/></div><p className="mt-1 text-sm text-muted-foreground">{payment.type}</p><p className="text-3xl font-bold">{formatNaira(payment.amount)}</p><Badge variant="secondary">{payment.status}</Badge></Card><Card className="p-5"><div className="flex flex-col gap-4 text-sm"><div className="flex justify-between"><span className="text-muted-foreground">Group</span><span className="font-medium">{payment.groupName}</span></div><div className="flex justify-between"><span className="text-muted-foreground">Reference</span><span className="font-medium">{payment.reference}</span></div><div className="flex justify-between"><span className="text-muted-foreground">Cycle</span><span className="font-medium">{payment.cycle??"—"}</span></div><div className="flex justify-between"><span className="text-muted-foreground">Date</span><span className="font-medium">{new Date(payment.date).toLocaleString()}</span></div></div></Card><Card className="p-5"><div className="flex items-center gap-3"><CreditCard className="size-5 text-primary"/><div><p className="font-semibold">Payment processing</p><p className="text-sm text-muted-foreground">MoneyCircle records contribution payments against your savings circle; it does not hold a wallet balance.</p></div></div></Card></PageShell>}
